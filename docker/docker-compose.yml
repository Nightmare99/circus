services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: circus
      POSTGRES_PASSWORD: circus
      POSTGRES_DB: circus
    ports:
      - "5432:5432"
    volumes:
      - circus-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U circus"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  circus-postgres-data:
